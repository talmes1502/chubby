# chub

`chub` is a hub for orchestrating multiple Claude Code CLI sessions. It provides a daemon, a CLI, and a wrapper around the Claude binary so you can launch, monitor, and interact with several concurrent Claude Code sessions from a single control plane.
